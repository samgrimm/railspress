<h1><%= t('adding') %> Website</h1>

<%= form_for(@website, url:websites_path( locale: I18n.locale) ) do |f| %>

  <%= render 'form',  f: f %>

  <div class="form-group">
    <%= f.submit t('.create'), class:'btn btn-primary' %>
  </div>
  <% end %>

<%= link_to fa_icon('arrow-circle-left 2x'), websites_path(locale:I18n.locale) %>
