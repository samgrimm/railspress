<h1><%= t('editing') %>  Navbar</h1>
<%= form_for(@navbar, url:website_page_path(@website, @navbar, locale: I18n.locale) ) do |f| %>

  <%= render 'form', f:f %>

  <div class="form-group">
    <%= f.submit t('.update'), class:"btn btn-primary" %>
  </div>
  <% end %>

<%= link_to fa_icon('folder-open-o'), website_navbar_path(@website,@navbar, locale:I18n.locale) %> |
<%= link_to fa_icon('arrow-circle-left'), website_navbars_path(@website, locale:I18n.locale) %>
